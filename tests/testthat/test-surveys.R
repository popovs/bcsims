# TEST TO WRITE

# Test that pagination consistently only returns on page,
# otherwise we are missing data

# Test that get_sims_surveys methods work - if a numeric
# is passsed, the numeric method is used; otherwise, default.

# Test that get_sims_surveys.numeric ignores `params`
